{
  "name": "queries-use-cat-fact",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/queries/use-cat-fact/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/queries/use-cat-fact",
        "main": "libs/queries/use-cat-fact/src/index.ts",
        "tsConfig": "libs/queries/use-cat-fact/tsconfig.lib.json",
        "assets": ["libs/queries/use-cat-fact/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/queries/use-cat-fact/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/queries/use-cat-fact/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
