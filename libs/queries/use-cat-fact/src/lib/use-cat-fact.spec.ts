import { queriesUseCatFact } from './queries-use-cat-fact';

describe('queriesUseCatFact', () => {
  it('should work', () => {
    expect(queriesUseCatFact()).toEqual('queries-use-cat-fact');
  });
});
